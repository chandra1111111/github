pipeline {
   agent any

    stages {
       stage ('complie stage') {
    
      steps {
       withMaven(maven : 'maven3.5.3) {
sh 'mvn clean compile'
}
}
}
 stage ('testing stage') {

   steps { 
    withMaven(maven : 'maven3.5.3') {
    sh 'mvn test'
}
}
}
